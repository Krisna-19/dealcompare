[pytest]
testpaths = tests
# Deterministic by default: tests marked `live` (real network / Amazon)
# are deselected. Run them explicitly with:  pytest -m live
addopts = -m "not live"
markers =
    live: hits real external services over the network (deselected by default)
